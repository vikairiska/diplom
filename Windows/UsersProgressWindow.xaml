<Window x:Class="VerhozinaIvanovDiplom.Windows.UsersProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Прогресс пользователей"
        Height="560"
        Width="1040"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource PrimaryTextBrush}"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="UsersListItemStyle" TargetType="ListViewItem">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="#141B26"/>
            <Setter Property="BorderBrush" Value="#2A3948"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}"
                                                  Columns="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=View.Columns}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#274457"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#111827"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#3B5C78"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ItemBorder" Property="Background" Value="#111827"/>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Прогресс пользователей" FontSize="24" FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="0,12,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="160"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="140"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Специализация:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="FilterSpecialtyCombo"
                      Grid.Column="1"
                      Height="36"
                      SelectionChanged="FilterCombo_SelectionChanged"/>
            <TextBlock Grid.Column="3" Text="Город:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="FilterCityCombo"
                      Grid.Column="4"
                      Height="36"
                      SelectionChanged="FilterCombo_SelectionChanged"/>
        </Grid>

        <ListView x:Name="UsersListView"
                  Grid.Row="2"
                  Margin="0,4,0,12"
                  ItemContainerStyle="{StaticResource UsersListItemStyle}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Фото" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="48" Height="48" CornerRadius="24" BorderBrush="#2A3948" BorderThickness="1" Background="#141B26">
                                    <Image Source="{Binding Photo}" Stretch="UniformToFill"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Логин" DisplayMemberBinding="{Binding Login}" Width="140"/>
                    <GridViewColumn Header="Специализация" DisplayMemberBinding="{Binding Specialty}" Width="200"/>
                    <GridViewColumn Header="Город" DisplayMemberBinding="{Binding City}" Width="110"/>
                    <GridViewColumn Header="Попыток" DisplayMemberBinding="{Binding Attempts}" Width="90"/>
                    <GridViewColumn Header="Успешных" DisplayMemberBinding="{Binding Passed}" Width="90"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Открыть прогресс"
                    Width="170"
                    Height="35"
                    Margin="0,0,10,0"
                    Style="{StaticResource ButtonStyleGreen}"
                    Click="OpenProgressButton_Click"/>
            <Button Content="Закрыть"
                    Width="120"
                    Height="35"
                    Style="{StaticResource ButtonStyleGreen}"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
